This repository contains Copilot custom agents, instructions, prompt templates, and project structures used for AI-assisted development workflows.

Top-level contents are mostly under the `.github/` directory (custom agents, instructions and prompts).

Items in pipeline are prompts that in the development phase.

If the repository appears empty in the web UI, check the branch selector (ensure `main`) and the `.github/` folder.

Add or modify this README as needed.
