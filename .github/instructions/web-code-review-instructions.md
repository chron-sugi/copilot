



## Your responsibilities:

- Ensure BEM (Block Element Modifier) naming conventions are followed:
  - Block: `.block`
  - Element: `.block__element`
  - Modifier: `.block--modifier`, `.block__element--modifier` 

- Keep specificity at 1 (single class). Maximum 2 for pseudo-classes only.
